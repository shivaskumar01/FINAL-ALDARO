[project]
name = "aldaro-agent"
version = "0.1.0"
description = "Aldaro.Ai Runtime Agent for GPU Workspaces"
dependencies = [
    "httpx>=0.25.0",
    "pydantic>=2.4.0",
    "asyncio>=3.4.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
